// The entry point, loaded by index.html. No bundler in this stack, so this is
// plain JS the browser runs directly; `pnpm check` type-checks *.ts/spec/scripts
// but not this file. Delete it and the script tag that loads it if the week's
// spec rules out JavaScript.
void 0;
